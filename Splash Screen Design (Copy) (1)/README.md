
  # Splash Screen Design (Copy)

  This is a code bundle for Splash Screen Design (Copy). The original project is available at https://www.figma.com/design/gBOq1bJnmkEt9rLLqkJbBv/Splash-Screen-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  